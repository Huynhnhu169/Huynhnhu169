# Assets

This folder is reserved for optional profile visuals, such as:

- a lightweight banner image
- a project collage
- a future demo screenshot

Keep assets small and avoid adding private information, API keys, local paths, or large generated files.

